spring:
  application:
    name: robert-ui

  threads:
    virtual:
      enabled: true

vaadin:
  launch-browser: true
  allowed-packages: com.vaadin,org.vaadin

---

spring:
  config:
    activate:
      on-profile: local

server:
  port: 8081
